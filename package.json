{
  "name": "movieinfo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:react": "npm install --prefix react && npm run build --prefix react",
    "build:next": " npm install --prefix next && npm run build --prefix next",
    "build": "npm run build:react && npm run build:next"
  }
}
