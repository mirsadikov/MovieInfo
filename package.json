{
  "name": "movieinfo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install --prefix react && npm install --prefix next",
    "next:build": "npm run build --prefix next",
    "react:build": "npm run build --prefix react",
    "build": "npm run react:build && npm run next:build"
  }
}
